# certs/

사설 CA 를 쓰는 네트워크(사내 프록시, MITM 검사 게이트웨이 등)에서 이미지를 빌드할 때
`.crt` 파일을 이 디렉터리에 넣으면 빌드 단계의 신뢰 목록에 추가된다.

비어 있으면 아무 일도 일어나지 않으므로, 일반적인 환경(GitHub Actions 포함)에서는 신경 쓸 필요가 없다.
인증서 파일 자체는 `.gitignore` 로 커밋에서 제외한다.
